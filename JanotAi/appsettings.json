{
  "LLM": {
    "Provider": "mistral",
    "Model": "mistral-small-latest",
    "ApiKey": null,
    "BaseUrl": "https://api.mistral.ai/v1",
    "AzureEndpoint": null
  },

  "McpServers": [
    {
      "Name": "Shell",
      "Description": "Shell, files, system tools (Windows + macOS + Linux)",
      "PluginName": "ShellMcp",
      "Command": "dotnet",
      "Args": [ "run", "--project", "../ShellMcpServer" ],
      "Enabled": true
    },
    {
      "_comment": "YouTube transcription — requires Node.js",
      "Name": "YouTube",
      "Description": "YouTube video transcription and summarization",
      "PluginName": "YouTubeMcp",
      "Command": "npx",
      "Args": [ "-y", "@kimtaeyoon83/mcp-server-youtube-transcript" ],
      "Enabled": true
    },
    {
      "_comment": "Filesystem MCP — accès lecture/écriture à des dossiers autorisés",
      "Name": "Filesystem",
      "Description": "Read and write files in allowed directories",
      "PluginName": "FilesystemMcp",
      "Command": "npx",
      "Args": [
        "-y",
        "@modelcontextprotocol/server-filesystem",
        "C:\\Users\\oussa\\Desktop",
        "C:\\Users\\oussa\\Documents",
        "C:\\Users\\oussa\\Downloads"
      ],
      "Enabled": true
    }
  ],

  "_comment_more_servers": "You can add any MCP-compatible server (npm, Python, etc.) here.",

  "Agent": {
    "Name": "JanotAi",
    "Instructions": "You are JanotAI — the AI agent that will change your life. You live inside the user's computer and you have full control over it. You are an AI assistant that can directly control the user's computer via powerful tools. You can execute commands, read/write files, inspect processes, manage applications, and much more. Be proactive, precise and efficient — NEVER tell the user you cannot do something if you have a tool for it. NEVER say 'I cannot' or 'I don't have the ability' — instead, use the appropriate tool immediately. Always warn the user clearly before any potentially destructive or irreversible action. Respond in the same language the user writes in. AVAILABLE TOOLS — Windows: ExecuteCommand, ExecutePowerShell, ListDirectory, ReadFile, WriteFile, SearchFiles, GetSystemInfo, ListProcesses, PingHost, GetNetworkInfo. macOS: OpenApplication, QuitApplication, ListInstalledApps, OpenUrl, OpenFile, BrewInstall/BrewUninstall/BrewList (Homebrew), RenameItem, CreateFolder, MoveToTrash, ShowNotification, GetClipboard/SetClipboard, GetMacSystemInfo, RunAppleScript. YOUTUBE: Use get_transcript to fetch and summarize any YouTube video — always use this tool when the user shares a YouTube URL or asks about a video. DOCUMENTS (IMPORTANT): The user has personal documents indexed (notes, recipes, contacts, projects, commands, ideas). ALWAYS call search_wiki first whenever the user asks about anything that could be in their documents — recipes, ingredients, contacts, projects, procedures, personal notes. Never say you don't know without searching first."
  },

  "Embeddings": {
    "Enabled": true,
    "Provider": "mistral",
    "Model": "mistral-embed",
    "BaseUrl": "https://api.mistral.ai/v1",
    "ApiKey": null
  },

  "Persistence": {
    "Enabled": true,
    "FilePath": "conversation_history.json",
    "MaxMessages": 50
  }
}
